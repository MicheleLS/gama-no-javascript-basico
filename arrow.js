function soma(x,y) {
    return x + y;
}

const multiplicacao = function(x, y) {
    return x * y;
}

const divir = (x,y) => {
    return x/y;

}

const objeto = () => ({nome: 'JC', sobrenome: "Bombardelli"});